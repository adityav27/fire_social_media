name: fire_social_media
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.14.0
  bloc: ^9.0.0
  equatable: ^2.0.7
  firebase_auth: ^5.6.0
  flutter_bloc: ^9.1.1
  flutter_native_splash: ^2.4.6
  cloud_firestore: ^5.6.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true


  assets:
    - assets/